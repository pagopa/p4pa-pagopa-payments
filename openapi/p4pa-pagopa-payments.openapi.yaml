openapi: 3.0.1
info:
  title: p4pa-pagopa-payments
  description: Api and Models
  version: 0.0.1
servers:
  - url: http://localhost:8080
    description: Generated server url
paths:
  /aca/create:
    post:
      deprecated: true
      description: Use [/aca/sync](#/aca/sync/syncAca) instead
      tags:
        - aca
      operationId: createAca
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DebtPositionDTO"
        required: true
      responses:
        "200":
          description: IUD inserted on ACA
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
        "400":
          description: Invalid request
        "500":
          description: Internal server error
  /aca/update:
    post:
      deprecated: true
      description: Use [/aca/sync](#/aca/sync/syncAca) instead
      tags:
        - aca
      operationId: updateAca
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DebtPositionDTO"
        required: true
      responses:
        "200":
          description: IUD updated on ACA
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
        "400":
          description: Invalid request
        "500":
          description: Internal server error
  /aca/delete:
    post:
      deprecated: true
      description: Use [/aca/sync](#/aca/sync/syncAca) instead
      tags:
        - aca
      operationId: deleteAca
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DebtPositionDTO"
        required: true
      responses:
        "200":
          description: IUD deleted on ACA
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
        "400":
          description: Invalid request
        "500":
          description: Internal server error
  /aca/sync:
    post:
      tags:
        - aca
      operationId: syncAca
      parameters:
        - name: iud
          in: query
          description: iud of installment to sync with ACA
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DebtPositionDTO"
        required: true
      responses:
        "204":
          description: No content
        "400":
          description: Invalid request
        "500":
          description: Internal server error
  /paymentsReporting/{organizationId}:
    get:
      tags:
        - payments-reporting
      operationId: getPaymentsReportingList
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: List of payments reporting
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PaymentsReportingIdDTO"
        "400":
          description: Invalid request
        "500":
          description: Internal server error
  /paymentsReporting/{organizationId}/fetch:
    get:
      tags:
        - payments-reporting
      operationId: fetchPaymentReporting
      parameters:
        - name: organizationId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: flowId
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                type: string
        "400":
          description: Invalid request
        "500":
          description: Internal server error
security:
  - BearerAuth: []
components:
  schemas:
    DebtPositionDTO:
      required:
        - organizationId
        - debtPositionTypeOrgId
        - paymentOptions
      type: object
      properties:
        debtPositionId:
          type: integer
          format: int64
        iupdOrg:
          type: string
        description:
          type: string
        status:
          $ref: "#/components/schemas/DebtPositionStatus"
        debtPositionOrigin:
          type: string
        ingestionFlowFileId:
          type: integer
          format: int64
        ingestionFlowFileLineNumber:
          type: integer
          format: int64
        organizationId:
          type: integer
          format: int64
        debtPositionTypeOrgId:
          type: integer
          format: int64
        notificationDate:
          type: string
          format: date-time
        validityDate:
          type: string
          format: date-time
        flagIuvVolatile:
          type: boolean
        creationDate:
          type: string
          format: date-time
        updateDate:
          type: string
          format: date-time
        paymentOptions:
          type: array
          items:
            $ref: "#/components/schemas/PaymentOptionDTO"
    PaymentOptionDTO:
      required:
        - installments
        - totalAmountCents
        - paymentOptionType
      type: object
      properties:
        paymentOptionId:
          type: integer
          format: int64
        debtPositionId:
          type: integer
          format: int64
        totalAmountCents:
          type: integer
          format: int64
        status:
          $ref: "#/components/schemas/PaymentOptionStatus"
        multiDebtor:
          type: boolean
        dueDate:
          type: string
          format: date-time
        description:
          type: string
        paymentOptionType:
          type: string
          enum:
            - SINGLE_INSTALLMENT
            - INSTALLMENTS
            - DOWN_PAYMENT
        installments:
          type: array
          items:
            $ref: "#/components/schemas/InstallmentDTO"
    InstallmentDTO:
      required:
        - amountCents
        - debtor
        - remittanceInformation
        - transfers
      type: object
      properties:
        installmentId:
          type: integer
          format: int64
        paymentOptionId:
          type: integer
          format: int64
        status:
          $ref: "#/components/schemas/InstallmentStatus"
        debtPositionOrigin:
          $ref: "#/components/schemas/DebtPositionOrigin"
        syncStatus:
          $ref: "#/components/schemas/InstallmentSyncStatus"
        iupdPagopa:
          type: string
        iud:
          type: string
        iuv:
          type: string
        iur:
          type: string
        iuf:
          type: string
        nav:
          type: string
        dueDate:
          type: string
          format: date-time
        paymentTypeCode:
          type: string
        amountCents:
          type: integer
          format: int64
        notificationFeeCents:
          type: integer
          format: int64
        remittanceInformation:
          type: string
        humanFriendlyRemittanceInformation:
          type: string
        balance:
          type: string
        legacyPaymentMetadata:
          type: string
        debtor:
          $ref: "#/components/schemas/PersonDTO"
        transfers:
          type: array
          items:
            $ref: "#/components/schemas/TransferDTO"
        creationDate:
          type: string
          format: date-time
        updateDate:
          type: string
          format: date-time
    TransferDTO:
      required:
        - amountCents
        - category
        - orgFiscalCode
        - orgName
        - remittanceInformation
      type: object
      properties:
        transferId:
          type: integer
          format: int64
        installmentId:
          type: integer
          format: int64
        orgFiscalCode:
          type: string
        orgName:
          type: string
        amountCents:
          type: integer
          format: int64
        remittanceInformation:
          type: string
        stampType:
          type: string
        stampHashDocument:
          type: string
        stampProvincialResidence:
          type: string
        iban:
          type: string
        postalIban:
          type: string
        category:
          type: string
        transferIndex:
          type: integer
          format: int32
    ReceiptDTO:
      type: object
      required:
        - receiptSource
        - paymentReceiptId
        - noticeNumber
        - orgFiscalCode
        - outcome
        - creditorReferenceId
        - paymentAmountCents
        - description
        - companyName
        - idPsp
        - pspCompanyName
        - idChannel
        - channelDescription
        - debtor
        - payer
      properties:
        receiptId:
          type: integer
          format: int64
        installmentId:
          type: integer
          format: int64
        ingestionFlowFileId:
          type: integer
          format: int64
        receiptOrigin:
          type: string
        paymentReceiptId:
          type: string
        noticeNumber:
          type: string
        paymentNote:
          type: string
        orgFiscalCode:
          type: string
        outcome:
          type: string
        creditorReferenceId:
          type: string
        paymentAmountCents:
          type: integer
          format: int64
        description:
          type: string
        companyName:
          type: string
        officeName:
          type: string
        idPsp:
          type: string
        pspFiscalCode:
          type: string
        pspPartitaIva:
          type: string
        pspCompanyName:
          type: string
        idChannel:
          type: string
        channelDescription:
          type: string
        paymentMethod:
          type: string
        feeCents:
          type: integer
          format: int64
        paymentDateTime:
          type: string
          format: date-time
        applicationDate:
          type: string
          format: date-time
        transferDate:
          type: string
          format: date-time
        standin:
          type: boolean
        debtor:
          $ref: "#/components/schemas/PersonDTO"
        payer:
          $ref: "#/components/schemas/PersonDTO"
        creationDate:
          type: string
          format: date-time
        updateDate:
          type: string
          format: date-time
    PersonDTO:
      required:
        - email
        - fullName
        - entityType
        - fiscalCode
      type: object
      properties:
        entityType:
          type: string
          enum:
            - F
            - G
        fiscalCode:
          type: string
        fullName:
          type: string
        address:
          type: string
        civic:
          type: string
        postalCode:
          type: string
        location:
          type: string
        province:
          type: string
        nation:
          type: string
        email:
          type: string
    DebtPositionStatus:
      type: string
      enum:
        - TO_SYNC
        - REPORTED
        - PAID
        - PARTIALLY_PAID
        - CANCELLED
        - INVALID
        - EXPIRED
        - UNPAID
        - DRAFT
    DebtPositionOrigin:
      type: string
      enum:
        - ORDINARY
        - SPONTANEOUS
        - SECONDARY_ORG
        - RECEIPT_FILE
        - RECEIPT_PAGOPA
        - REPORTING_PAGOPA
    PaymentOptionStatus:
      type: string
      enum:
        - TO_SYNC
        - REPORTED
        - PAID
        - PARTIALLY_PAID
        - CANCELLED
        - INVALID
        - EXPIRED
        - UNPAID
        - DRAFT
    InstallmentStatus:
      type: string
      enum:
        - TO_SYNC
        - REPORTED
        - PAID
        - CANCELLED
        - INVALID
        - EXPIRED
        - UNPAID
        - DRAFT
    SyncStatusUpdateRequestDTO:
      type: object
      additionalProperties:
        $ref: "#/components/schemas/IupdSyncStatusUpdateDTO"
    IupdSyncStatusUpdateDTO:
      type: object
      properties:
        newStatus:
          $ref: "#/components/schemas/InstallmentStatus"
        iupdPagopa:
          type: string
    InstallmentSyncStatus:
      required:
        - syncStatusFrom
        - syncStatusTo
      type: object
      properties:
        syncStatusFrom:
          $ref: "#/components/schemas/InstallmentStatus"
        syncStatusTo:
          $ref: "#/components/schemas/InstallmentStatus"
    PaymentsReportingIdDTO:
      required:
        - pagopaPaymentsReportingId
        - flowDateTime
        - paymentsReportingFileName
      type: object
      properties:
        pagopaPaymentsReportingId:
          type: string
        flowDateTime:
          type: string
          format: date-time
        paymentsReportingFileName:
          type: string
    PagoPaPaymentsErrorDTO:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          enum:
            - PAGOPA_PAYMENTS_NOT_FOUND
            - PAGOPA_PAYMENTS_BAD_REQUEST
            - PAGOPA_PAYMENTS_GENERIC_ERROR
        message:
          type: string
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
